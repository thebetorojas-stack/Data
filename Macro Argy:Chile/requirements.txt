# Pinned for Python 3.8 compatibility.
# These are the last versions that still support 3.8 — newer releases require 3.9+.

# Core data + storage
pandas>=1.5,<2.1                # 2.0.x is the last to support Py3.8
numpy>=1.24,<1.25               # 1.25+ requires Py3.9+
pyarrow>=14.0,<15.0             # 15+ requires Py3.9+
pyyaml>=6.0
python-dateutil>=2.8

# Bloomberg
xbbg>=0.7.7
blpapi>=3.20

# Haver Analytics — installs with DLX, not from pip.
# If `import Haver` fails, ensure DLX is installed and Haver's Python folder
# (typically C:\DLX\Data) is on PYTHONPATH.

# Excel output
openpyxl>=3.1,<3.2
xlsxwriter>=3.1

# Dashboard
streamlit>=1.28,<1.34           # 1.34+ requires Py3.9+
plotly>=5.18,<5.20              # 5.20+ requires Py3.9+
altair>=4.2,<5.2                # streamlit dep, pin for 3.8

# CLI / logging
click>=8.1
rich>=13.7,<14.0
